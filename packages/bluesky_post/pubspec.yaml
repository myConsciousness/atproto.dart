name: bluesky_post
description: A server app using the shelf package and Docker.
version: 2.0.0
environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  actions_toolkit_dart: ^0.5.1
  bluesky: ^0.2.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
